server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/walletdb
    username: wallet
    password: wallet
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
